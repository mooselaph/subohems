# Test App

This workspace contains a minimal React frontend (Vite) and an Express backend.

Quick start (frontend dev):

```bash
cd frontend
npm install
npm run dev
```

Quick start (backend):

```bash
cd backend
npm install
npm start
```

To serve production build:

```bash
cd frontend
npm run build
cd ../backend
npm start
```
